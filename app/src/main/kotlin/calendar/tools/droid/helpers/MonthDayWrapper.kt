package calendar.tools.droid.helpers

import calendar.tools.droid.models.MonthDayApiModel

class MonthDayWrapper {

    val dayCodeList: List<MonthDayApiModel> = ArrayList()

    /*fun getMonthDayApiModel(dayCode:String){
        dayCodeList.stream().filter()
        dayCodeList.forEach {
            if()
        }
    }*/
}